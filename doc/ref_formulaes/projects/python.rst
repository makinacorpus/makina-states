Python project
==============

Install a python project. "python_version" can be passed to the install macro
to change the python version used by the project.

If requirement is set to True (default), then it will install the requirements
from the requirements.txt in the project directory using pip.


