# MANAGED VIA SALT -- DO NOT EDIT
{% set data = salt['mc_utils.json_load'](data) %}

/**
 *  The livestatus library implements the livestatus query protocol.
 */

library "livestatus"

{% for name,socket in data.modules.mklivestatus.sockets.items() %}
object LivestatusListener "{{name}}" {
    {% for key, value in socket.items() %}
    {{key}} = "{{value}}"
    {% endfor %}
}

{% endfor%}

