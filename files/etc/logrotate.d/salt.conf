{{log_prefix}}/{{salt_data['name']}}-syndic
{{log_prefix}}/{{salt_data['name']}}-master
{{log_prefix}}/{{salt_data['name']}}-minion
{{log_prefix}}/{{salt_data['name']}}-master-key
{{log_prefix}}/{{salt_data['name']}}-minion-key
{{log_prefix}}/*.log {
  daily
  copytruncate
  missingok
  rotate {{salt_data['rotate'] }}
  compress
  notifempty
}
