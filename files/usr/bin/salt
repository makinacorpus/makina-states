#!/usr/bin/env bash
export PATH="{{salt_data['salt_root']}}/makina-states/bin:$PATH"
if [[  $1 == "*" ]];then
    shift
    exec salt -c {{salt_data['conf_prefix']}} '*' $@
else
    exec salt -c {{salt_data['conf_prefix']}} $@
fi
exit $?
# vim:set ft=bash:
