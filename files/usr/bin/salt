#!/usr/bin/env bash
export PATH="{{salt_data['salt_root']}}/makina-states/bin:$PATH"
if [[  $1 == "*" ]];then
    shift
    salt -c {{salt_data['conf_prefix']}} '*' $@
else
    salt -c {{salt_data['conf_prefix']}} $@
fi
# vim:set ft=bash:
