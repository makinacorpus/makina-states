#!/usr/bin/env bash
export PATH="{{root}}/bin:$PATH"
if [[  $1 == "*" ]];then
    shift
    {{root}}/bin/salt -c {{saltconfig}} '*' $@
else
    {{root}}/bin/salt -c {{saltconfig}} $@
fi
# vim:set ft=bash:
