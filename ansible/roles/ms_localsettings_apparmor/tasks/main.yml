---
- name: "ms_localsettings_apparmor preflight check - SKIPTEST"
  debug:
    msg: '{{True}}'
  register: ms_localsettings_apparmor_can_run
  tags: ms_localsettings_apparmor
- include: run.yml
  when: '{{ ms_localsettings_apparmor_can_run.msg == True }}'
  tags: ms_localsettings_apparmor
